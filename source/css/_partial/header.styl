.header {
  height 50px;
  position relative;
  box-shadow 0 1px 6px rgba(0, 0, 0, .3);
};

.nav-box {
  display flex;
  justify-content flex-end;

  .nav-item {
    padding: 14px 20px;
    display: block;
  }
}