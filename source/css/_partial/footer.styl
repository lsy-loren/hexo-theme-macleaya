@import "variable.styl";

.footer {
  height 50px;
  margin-top 20px;
  line-height 50px;
  font-size 12px;
  position relative;
  box-shadow 0 1px 6px rgba(0, 0, 0, .3);

  .footer-box {
    text-align center;
  }
};
